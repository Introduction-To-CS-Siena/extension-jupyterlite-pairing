{
  "name": "csis110-pairing-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@hocuspocus/server": "4.6.0",
    "y-protocols": "1.0.7",
    "yjs": "13.6.32"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260828.1",
    "typescript": "5.9.3",
    "wrangler": "4.127.1"
  },
  "engines": {
    "node": ">=22"
  }
}

